SELECT Essn,DPRT.Dnumber,count(*) FROM DEPENDENT D INNER JOIN DEPARTMENT DPRT ON D.Essn = DPRT.Mgr_ssn WHERE Essn IN (SELECT Mgr_ssn FROM DEPARTMENT WHERE DNumber IN (SELECT Dnum FROM PROJECT GROUP BY Dnum HAVING count(distinct Plocation) > 1)) GROUP BY Essn;
